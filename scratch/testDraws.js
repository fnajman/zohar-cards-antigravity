const fetch = require('node-fetch');
const fs = require('fs');

async function test() {
  const token = 'YOUR_TEST_TOKEN'; // We need a real token. Wait, I can just use a real auth request or I can check the user's `testXano.js`? Wait, I saw a `testXano.js` in scratch earlier.
}
test();
